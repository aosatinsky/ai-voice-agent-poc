{
  "name": "agustin-pizzeria",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "init-db": "wrangler d1 execute pizzeria-db --file=./schema.sql",
    "init-db:remote": "wrangler d1 execute pizzeria-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
